* tests för overrideSattr
* lägg till en Environmentklass med strängar för conf å annat
fluent...
* loggning för fluent
* Verifiera typ cfg.Audit<foo>().ExcludeRelationData(foo => foo.MyComponent.Bar)